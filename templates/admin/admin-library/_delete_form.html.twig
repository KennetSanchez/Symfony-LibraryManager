<form method="post" action="{{ path('deleteLibrary', {'id': library.id}) }}" onsubmit="return confirm('¿Quieres eliminar esta biblioteca?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ library.id) }}">
    <button class="btn btn-danger mt-3">Eliminar</button>
</form>
