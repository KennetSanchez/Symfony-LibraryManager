<div class="">
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="d-flex justify-content-evenly container">
        <a class="btn btn-outline-secondary" style="width: 44%" href="{{ path('booksIndex') }}">Volver al listado</a>
        <button  type="submit" class="btn btn-primary" style="width: 44%">{{ button_label|default('Save') }}</button>

    </div>

    {{ form_end(form) }}
</div>
