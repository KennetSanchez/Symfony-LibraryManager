{{ form_start(form) }}
    {{ form_widget(form) }}

<div class="d-flex justify-content-evenly container w-75">
    <a class="btn btn-outline-secondary w-25" href="{{ path('booksIndex') }}">Volver al listado</a>
    <button  type="submit" class="btn btn-primary w-25">{{ button_label|default('Save') }}</button>

</div>

{{ form_end(form) }}
