<form method="POST" action="{{ path('deleteBook', {'id': book.id}) }}" onsubmit="return confirm('¿Estás seguro de querer eliminar este libro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>